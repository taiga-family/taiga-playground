name: ⚙️ CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: taiga-family/ci/actions/setup/variables@11d368eb2bd162fe7d2dda36b48d3dd381cfadc5 # v1.101.0
      - uses: taiga-family/ci/actions/setup/node@11d368eb2bd162fe7d2dda36b48d3dd381cfadc5 # v1.101.0
      - run: npx ng build
      - run: |
          if [[ "${{ env.SUPPORT_AUTO_PUSH }}" == "true" ]]; then
              npx stylelint '**/*.{less,css}' --fix
              npx prettier '**/*.*' --write
              npx eslint . --fix
          else
              npx stylelint '**/*.{less,css}'
              npx prettier '**/*.*' --check
              npx eslint .
          fi
      - uses: taiga-family/ci/actions/auto/push@11d368eb2bd162fe7d2dda36b48d3dd381cfadc5 # v1.101.0
        with:
          token: ${{ secrets.TAIGA_FAMILY_BOT_PAT }}

concurrency:
  group: build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
